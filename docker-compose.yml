version: "3.7"
services:

  app:
    build: .
    expose:
      - "8765"
    ports:
      - "8765:8765"
    environment:
      PORT: 8765
      MONGOURI: 'mongodb+srv://dbTestuser:<password>@cluster0-zc3ak.mongodb.net/test?retryWrites=true&w=majority'
